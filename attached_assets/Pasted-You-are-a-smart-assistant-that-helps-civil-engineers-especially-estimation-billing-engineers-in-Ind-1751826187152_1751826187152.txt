You are a smart assistant that helps civil engineers, especially estimation/billing engineers in India, prepare Excel-based cost estimates for public works.

🔷 OBJECTIVE:
Build an MVP web interface that allows the user to:
1. Upload a basic Bill of Quantities (BOQ) or type it manually (item name, unit, quantity).
2. Select the appropriate State Schedule Rates (SSR)  PWD (Maharashtra etc.).
3. Fetch matching item rates from the SSR database (use VLOOKUP-style matching).
4. Auto-fill an Excel sheet with:
   - Serial No
   - Item Description
   - Unit
   - Quantity
   - Rate (from SSR)
   - Amount (Quantity × Rate)
   - Remarks (optional)

🔷 CORE FEATURES:
- Rate lookup using partial keyword match or item code (SSR)
- Excel sheet export that looks like municipal/public works billing format
- Predefined dropdown for units (m, m², m³, ft, kg, lump sum, etc.)
- Optional: Let the user input custom rate if SoR doesn’t have an entry

🔷 TECHNICAL REQUIREMENTS:
- Backend: Can be Node.js with ExcelJS, pandas, or openpyxl
- Frontend: Lightweight interface using React/HTML (optional for MVP)
- Excel format: Should have borders, ₹ currency formatting, total at bottom

🔷 MVP SCOPE:
Only Phase 1 features:
- No user authentication or multi-user logic
- No database persistence (in-memory only or session-based)
- SSR source can be mocked or provided as PDF file. The app can then analyse the PDF SSR file and then use it to suggest using partial keyword match or item code (SSR)
- Excel export only (PDF optional in future)

🔷 Example BOQ Input:
| Item Description        | Unit | Quantity |
|-------------------------|------|----------|
| Excavation in soil      | m³   | 12.5     |
| PCC 1:4:8 cement mix    | m³   | 3.0      |
| Brickwork 230mm        | m³   | 5.5      |

🔷 Output (Excel):
| S.No | Description         | Unit | Qty  | Rate (₹) | Amount (₹) | Remarks |
|------|---------------------|------|------|----------|-------------|---------|
| 1    | Excavation in soil  | m³   | 12.5 | 180      | 2250.00     | As per SSR |
| 2    | PCC 1:4:8 cement mix| m³   | 3.0  | 600      | 1800.00     | Manual rate |
| 3    | Brickwork 230mm     | m³   | 5.5  | 750      | 4125.00     |           |
|      |                     |      |      | **Total**| ₹8175.00    |         |

Provide reusable code/functions or endpoints to build this assistant as a plug-and-play module.

Make sure you create the code in such a way that I can download the entire app, import it to VS Code or Cursor and then run some basic npm install for dependencies,and  npm start so that the app will be up and running on localhost





Good, thanks for the portal. Now, I want to have only the PWD for Maharashtra. Also, please find attached the SSR for PWD Maharashtra. Feed it to the database so that it can suggest the text with type-ahead functionality while adding the item description
(It's , so make a change accordingly on the webpage.)




